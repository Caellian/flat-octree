{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug unit tests in library 'flat-octree'",
      "cargo": {
        "args": ["test", "--no-run", "--lib", "--package=flat-octree"],
        "filter": {
          "name": "flat-octree",
          "kind": "lib"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
